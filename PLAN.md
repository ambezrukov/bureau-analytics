# PLAN.md — План дальнейшей работы над аналитикой бюро

> **Обновлён:** март 2026
> **Текущий статус:** MVP работает на Streamlit Cloud, февраль 2026 в базе

---

## ✅ ВЫПОЛНЕНО

- [x] Разведка данных (XLS-структура, 13 исходных периодов)
- [x] SQLite-база (`data/bureau_data.sqlite`)
- [x] Скрипт импорта v2.0 (`scripts/import_data.py`) через win32com.client
- [x] Шаблон справочника сотрудников (`data/Справочник_сотрудников.xlsx`)
- [x] Streamlit-дашборд (5 страниц: Обзор, Сотрудники, Динамика, Проекты, ФОТ-заглушка)
- [x] Git-репозиторий + деплой на Streamlit Cloud
- [x] Colab-блокнот для ежемесячного обновления данных
- [x] GitHub MCP подключён к Claude Desktop
- [x] Схема БД расширена до 47 полей (февраль 2026+)
- [x] Февраль 2026 импортирован — итого **78 114 записей**, 15 периодов

---

## 🔜 БЛИЖАЙШИЕ ЗАДАЧИ (до переезда на VPS)

### 1. Заполнить справочник сотрудников
- Открыть `data/Справочник_сотрудников.xlsx`
- Заполнить: Рабочая группа, Уровень, Оклад, Целевая выработка, Целевая реализация, Дата приёма
- **Примечание:** начиная с февраля 2026 поле «Подразделение» заполняется автоматически
  из XLS-выгрузки. Для старых периодов (янв 2025 — янв 2026) по-прежнему нужен справочник.
- После заполнения окладов: разблокируется страница ФОТ

### 2. Ежемесячный импорт новых данных
Когда получена новая выгрузка из ProjectMate:
1. Переложить XLS-файл в `data/` (назвать `YYYY-MM.xls`)
2. Запустить: `python bureau-analytics/scripts/import_data.py`
3. Скопировать базу: `data/bureau_data.sqlite` → `bureau-analytics/dashboard/bureau_data.sqlite`
4. Запушить: `git add bureau_data.sqlite && git commit -m "Add YYYY-MM data" && git push`

---

## 🚀 ЭТАП 2 — ПЕРЕЕЗД НА VPS

### Когда делать
Когда нужны:
- Авторизация (не все должны видеть данные)
- Загрузка XLS через интерфейс (без терминала)
- Форма ввода нецифровых показателей от руководителей
- Полный контроль над окружением

### Что нужно от пользователя перед стартом
- [ ] Выбрать и арендовать VPS (рекомендую: Hetzner CX22 ~4€/мес или Timeweb ~200₽/мес)
- [ ] Ubuntu 22.04 LTS
- [ ] Получить IP-адрес и root-доступ по SSH
- [ ] Опционально: домен (можно обойтись IP на первое время)

### Задачи на VPS
1. Установка Python 3.11, nginx, certbot (HTTPS)
2. Настройка systemd-сервиса для Streamlit
3. Перенос базы данных
4. Настройка nginx как reverse proxy (порт 8501 → 80/443)
5. Подключение домена + SSL-сертификат (Let's Encrypt)

---

## 🔐 ЭТАП 3 — АВТОРИЗАЦИЯ

**Варианты (в порядке сложности):**

### Вариант A: streamlit-authenticator (быстро, ~2 часа)
- Логин/пароль в `secrets.toml`
- Подходит для 5–15 пользователей
- Не требует базы пользователей

### Вариант B: Keycloak / Google OAuth (надёжно, ~1 день)
- Полноценный SSO
- Роли: администратор, руководитель группы, просмотр
- Рекомендуется при росте команды

**Роли для бюро:**
- `admin` — видит всё, может загружать данные
- `group_lead` — видит свою группу + форму ввода показателей
- `viewer` — только просмотр

---

## 📤 ЭТАП 4 — ЗАГРУЗКА ДАННЫХ ЧЕРЕЗ ИНТЕРФЕЙС

**Цель:** секретарь загружает XLS без терминала

**Реализация:**
- Новая страница `6_⬆️_Загрузка.py` (доступна только роли `admin`)
- `st.file_uploader(type=['xls', 'xlsx'])`
- Вызов той же логики, что в `scripts/import_data.py`
- На VPS: xlrd работает нормально (win32com не нужен)
- Отчёт об импорте: сколько добавлено, сколько дублей пропущено

---

## 📝 ЭТАП 5 — ФОРМА НЕЦИФРОВЫХ ПОКАЗАТЕЛЕЙ

**Цель:** руководители групп вносят качественные оценки за период

**Планируемые поля:**
- Период (выбор из списка)
- Рабочая группа
- Ключевые события месяца (текст)
- Основные риски (текст)
- Планы на следующий период (текст)
- Оценка командного духа (1–5)
- Комментарий руководителя (текст)

**Хранение:** новая таблица `qualitative_reports` в SQLite

**Интеграция в дашборд:**
- На странице «Сотрудники» — вкладка «Комментарий руководителя»
- Экспорт в итоговый отчёт

---

## 💰 ЭТАП 6 — СТРАНИЦА ФОТ (после заполнения окладов)

**Что показывать:**
- Оклад vs фактические часы
- Стоимость часа по сотруднику
- Маржинальность по проектам (выручка − ФОТ)
- Сравнение план/факт по выработке

**Зависит от:** заполненного справочника с окладами

---

## 🔗 ЭТАП 7 — GITHUB MCP (✅ подключён)

GitHub MCP уже работает. Claude читает/пишет файлы в репозитории напрямую.

**Безопасность:**
- Работать в ветке `dev`, не в `master`
- `master` защитить через Branch Protection Rules (Settings → Branches)
- Claude делает PR → пользователь проверяет → merge вручную

**Структура веток:**
```
master   ← стабильная версия (защищена)
dev      ← текущая разработка
feature/ ← отдельные задачи (feature/fot-page, feature/auth, ...)
```

---

## 📊 МОНИТОРИНГ РОСТА БАЗЫ ДАННЫХ

| Объём базы | Действие |
|-----------|---------|
| Сейчас: ~60 МБ | Всё ок |
| ~90 МБ (~через 8 мес.) | Настроить Git LFS |
| >500 МБ | Рассмотреть PostgreSQL |
| >2 ГБ | Обязательно PostgreSQL |

**Как включить Git LFS:**
```bash
git lfs install
git lfs track "*.sqlite"
git add .gitattributes
git commit -m "Track SQLite with Git LFS"
```

---

## 🗂️ АРХИТЕКТУРНЫЕ ДЕТАЛИ ДЛЯ ПАМЯТИ

### Почему win32com.client вместо xlrd
XLS-файлы из ProjectMate >2MB не читаются через xlrd (ложно-повреждённые).
Решение работает только на Windows. На VPS (Linux) использовать xlrd или openpyxl.

### decimal.Decimal в новом формате выгрузки (фев 2026+)
Числовые поля в новом шаблоне возвращаются как `decimal.Decimal`, а не `float`.
SQLite3 их не принимает. В `clean_value()` добавлен конвертор:
```python
import decimal
if isinstance(value, decimal.Decimal):
    return float(value)
```

### Поле «Подразделение» в XLS
Начиная с февраля 2026 поле заполнено в выгрузках (раньше было пустым).
В `import_data.py` оно маппится: `'Подразделение' → 'department'`.
Для старых периодов — брать из справочника staff через COALESCE:
```sql
COALESCE(t.department, s.work_group) AS work_group_resolved
```

### Совместимость сумм (старый / новый формат)
- Старый формат: столбец «Сумма» → поле `amount`
- Новый формат: «Сумма, учтено» → `amount_recorded`; `amount` заполняется из `amount_recorded`
- Дашборд использует поле `amount` — работает с обоими форматами

### Курсы валют в данных (фев 2026+)
Поле `exchange_rate` теперь заполняется из столбца «Курс к учетной валюте (значения)».
Хардкод `USD_RATE, EUR_RATE = 88.0, 95.0` в дашборде пока остаётся,
но в будущем можно заменить на `SELECT AVG(exchange_rate) FROM time_entries WHERE currency=...`.

### Статус «Нома акта»
Опечатка в ProjectMate: столбец называется «Нома акта» (вместо «Номер акта»).
Маппится в поле `act_number`. Если в будущем исправят — добавить второй маппинг.

---

## 📞 КАК НАЧАТЬ НОВУЮ СЕССИЮ С CLAUDE

1. Открой проект `Z:\Мой диск\ИИ\employees_KPI` в Claude Desktop
2. Скажи: «Продолжаем работу над аналитикой бюро»
3. Claude прочитает этот файл с GitHub и сразу будет в контексте

---

*Файл обновляется агентом Claude после каждого значимого этапа.*
