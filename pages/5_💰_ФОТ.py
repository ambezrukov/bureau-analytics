"""
Страница «ФОТ» — фонд оплаты труда (в разработке).
"""

import sys
import pathlib
sys.path.insert(0, str(pathlib.Path(__file__).parent.parent))

import streamlit as st
from utils.data import render_sidebar

render_sidebar(key_prefix='fot_')

st.title("💰 ФОТ — Фонд оплаты труда")

st.info("""
🚧 **В разработке**

Этот раздел будет содержать:
- Оклады и премии по сотрудникам за каждый период
- Сравнение ФОТ с выработкой и реализацией
- Рентабельность (выручка / ФОТ)
- Динамика ФОТ по месяцам и группам

**Для запуска этого раздела необходимо:**
1. Заполнить поля «Оклад» в файле `Справочник_сотрудников.xlsx`
2. Загрузить данные через `python scripts/import_data.py`

Или подготовить файлы зарплатных выгрузок в формате Excel со столбцами:
`ФИО | Период | Оклад | Премия | Итого`
""")

st.divider()

st.markdown("""
### Как подключить данные ФОТ

1. Откройте файл `data/Справочник_сотрудников.xlsx`
2. Заполните столбец **Оклад (руб.)** для каждого сотрудника
3. При необходимости добавьте файл `data/Зарплаты.xlsx` со структурой:
   ```
   ФИО | Период | Оклад | Премия | Итого | Комментарий
   ```
4. Перезапустите скрипт: `python scripts/import_data.py`
5. Обновите страницу дашборда
""")
